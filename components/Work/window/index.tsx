'use client'

// This component is now largely redundant as the Canvas and View logic
// has been moved up to app/work/page.tsx.
// It is kept for now to avoid breaking imports, but it renders nothing.
export default function WorkWindow() {
  return null;
}
